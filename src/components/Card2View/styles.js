/* eslint-disable comma-dangle */
import { StyleSheet } from 'react-native';
import { card2 } from '../../AppStyles';

const styles = StyleSheet.create({
  itemContainer: card2.itemContainer,
  mainContainer: card2.mainContainer,
  rowContainer: card2.rowContainer,
  itemIcon: card2.itemIcon,
  itemTxtContainer: card2.itemTxtContainer,
  itemTitle: card2.itemTitle,
  itemText: card2.itemText,
  itemMoney: card2.itemMoney
});

export default styles;
