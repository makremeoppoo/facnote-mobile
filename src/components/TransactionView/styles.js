/* eslint-disable comma-dangle */
import { StyleSheet } from 'react-native';
import { transactions } from '../../AppStyles';

const styles = StyleSheet.create({
  itemContainer: transactions.itemContainer,
  mainContainer: transactions.mainContainer,
  rowContainer: transactions.rowContainer,
  txtContainer: transactions.txtContainer,
  itemIconContainer: transactions.itemIconContainer,
  itemIcon: transactions.itemIcon,
  itemTxtContainer: transactions.itemTxtContainer,
  itemTitleContainer: transactions.itemTitleContainer,
  itemTitle: transactions.itemTitle,
  itemDate: transactions.itemDate,
  itemMoney: transactions.itemMoney
});

export default styles;
